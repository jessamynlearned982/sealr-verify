# Sealr release root certificates.
#
# Development builds ship this placeholder (no roots embedded); the release
# pipeline replaces this file with the production Sealr root certificate(s)
# before building distributed verifier binaries. With no embedded root, the
# verifier falls back to --root or (with a warning) the bundle's own roots.
